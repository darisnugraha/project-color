const getAllReturBatu = (state) => state.returbatu.feedback;
const getErrorReturBatu = (state) => state.returbatu.error;
const getIsEditReturBatu = (state) => state.returbatu.isEdit;

const data = {
  getAllReturBatu,
  getErrorReturBatu,
  getIsEditReturBatu,
};

export default data;
